[pytest]
markers =
    readiness_suite: verifies build readiness and validates that the full test sweep passes from a fresh environment.
    integration: marks long-running or artifact-dependent integration checks.
